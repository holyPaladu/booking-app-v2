import { ConflictError, UnauthorizedError } from '@booking/shared'
import type { AuthLoginRequest, AuthRegisterRequest } from './models'
import type { IAuthRepo } from './ports/repo.port'

export const authService = (repo: IAuthRepo) => {

  return {
  }
}
