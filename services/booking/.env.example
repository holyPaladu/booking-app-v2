# PostgreSQL connection string (required)
DB_URL=postgres://postgres:postgres@localhost:5432/booking

# JWT — секрет ДОЛЖЕН совпадать с auth-сервисом (booking только проверяет токены)
JWT_SECRET=change-me-in-production

# HTTP
PORT=3001
NODE_ENV=development
